import React from 'react';
import '../../style/error.css';

const ErrorPage = () => {
    return (
        <div>
            <h1 className="error-message">You have landed on a non-existent page.</h1>
        </div>
    );
};

export default ErrorPage;